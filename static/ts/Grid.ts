import { ctx } from "./Main.js";
import { Room } from "./Room.js";

/** transforms an easy to use vector to a renderable one
 *  i.e world gets moved and scaled to centre of screen
*/
function transform(normal:Vec2):Vec2 {
    return normal
}

export function devRenderGround() {
    
}

export function devRenderRoom(room: Room) {
    
}